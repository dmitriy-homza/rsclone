import React from 'react';
// import PropTypes from 'prop-types';
import BasketElement from './basketElement';

const Basket = () => (
  <>
    <h2>Basket</h2>
    <h2>{}</h2>
    <BasketElement />
  </>
);

// Basket.propTypes = {
//   selectedAdditional1: PropTypes.arrayOf(PropTypes.string).isRequired,
// };

export default Basket;
